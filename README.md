# kulus-legal

Legal pages for the Kulus glucose-tracking app. `privacy.html` is the privacy policy; `index.html` redirects to it.

## Stack

Static HTML with inline styles.

## Build & run

No build step. Open `privacy.html` in a browser, or serve the directory:

```sh
python3 -m http.server
```
